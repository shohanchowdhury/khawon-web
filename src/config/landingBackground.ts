export const LANDING_PATTERN = {
  src: '/background/all.png',
  /** Tile size on repeat grid — smaller values = more zoomed-out (finer) pattern. */
  tileWidth: 708,
  tileHeight: 386,
  mobileTileWidth: 419,
  mobileTileHeight: 228,
  duration: 52,
} as const
